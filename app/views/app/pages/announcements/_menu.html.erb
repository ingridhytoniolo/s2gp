<div class="hidden lg:block w-full">
  <div class="tabs">
    <a href="<%= app_announcements_path %>" <%= 'class=active' if submenu.nil? %>>
      <%= t('pages.app.pages.announcements.active') %>
    </a>
    <a href="<%= app_announcements_path %>?show=all" <%= 'class=active' if submenu == 'all' %>>
      <%= t('pages.app.pages.announcements.all') %>
    </a>
    <a href="<%= app_announcements_path %>?show=mine" <%= 'class=active' if submenu == 'mine' %>>
      <%= t('pages.app.pages.announcements.mine') %>
    </a>
  </div>
</div>
<div class="lg:hidden w-full">
  <div class="mobile-tabs">
    <select name="submenu" id="submenu" onchange="redirect(this)">
      <option class="submenu-item" value="announcement" data-href="<%= app_announcements_path %>" <%= 'selected' if submenu.nil? %>><%= t('pages.app.pages.announcements.active') %></option>
      <option class="submenu-item" value="announcement" data-href="<%= app_announcements_path %>?show=all" <%= 'selected' if submenu == 'all'%>><%= t('pages.app.pages.announcements.all') %></option>
      <option class="submenu-item" value="announcement" data-href="<%= app_announcements_path %>?show=mine" <%= 'selected' if submenu == 'mine' %>><%= t('pages.app.pages.announcements.mine') %></option>
    </select>
  </div>
</div>
<script>
  function redirect(item) {
    var selected = item.options[item.selectedIndex];
  
    window.location.href = selected.dataset.href;
  }
</script>
