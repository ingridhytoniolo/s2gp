<!DOCTYPE html>
<html lang="<%= locale %>">
  <head>
    <title><%= @group[:name] %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>
  <body>
    <h1><%= @meeting.title %></h1>
    <p><%= @meeting.date.strftime('%d/%m/%Y %H:%M') %></p>
    <br />
    <p><%= @meeting.minutes&.gsub(/\n/, '<br/>')&.html_safe %></p>
  </body>
</html>
