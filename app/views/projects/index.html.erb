<%= render 'layouts/header' %>
<div class="container mx-auto p-4">
  <div class="mb-4 text-center">
    <h1><%= t('pages.projects.title') %></h1>
    <h2><%= t('pages.projects.sub_title') %></h2>
  </div>
  <div class="flex flex-wrap justify-center">
    <% @projects.each do |project| %>
      <div class="p-4 w-full lg:w-1/3 md:w-1/2">
        <div class="h-full border-2 border-gray-200 border-opacity-60 rounded-lg overflow-hidden">
          <img class="h-48 w-full object-cover object-center" src="<%= project[:image_url] %>" alt="">
          <div class="p-6">
            <h2 class="tracking-widest text-xs font-medium text-gray-400 mb-1">
              <%= project[:status] %>
            </h2>
            <h1 class="text-lg font-medium text-gray-900 mb-3">
              <%= project[:title] %>
            </h1>
            <p class="text-sm leading-relaxed mb-3">
              <%= project[:main_goal] %>
            </p>
            <div class="flex items-center">
              <a href="#" class="inline-flex items-center md:mb-2 lg:mb-0">
                <%= t('pages.projects.buttons.open') %>
                <svg class="w-4 h-4 ml-2" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M5 12h14"></path>
                  <path d="M12 5l7 7-7 7"></path>
                </svg>
              </a>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
